language:
  - cpp
  
sudo: required
dist: trusty
  
compiler: 
 - gcc
 - clang

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - clang
      - gcc-5
      - g++-5
      - libirrlicht-dev
      - libbullet-dev
      - libfreetype6-dev

script:
  - make
